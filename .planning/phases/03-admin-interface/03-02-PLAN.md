---
phase: 03-admin-interface
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - shifttracker/admin/routers/groups.py
  - shifttracker/admin/routers/employees.py
  - shifttracker/admin/routers/caption_rules.py
  - shifttracker/admin/router.py
  - shifttracker/templates/admin/groups/list.html
  - shifttracker/templates/admin/groups/form.html
  - shifttracker/templates/admin/employees/list.html
  - shifttracker/templates/admin/employees/form.html
  - shifttracker/templates/admin/employees/_bindings.html
  - shifttracker/templates/admin/caption_rules/list.html
  - shifttracker/templates/admin/caption_rules/form.html
  - tests/test_admin_crud.py
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05

must_haves:
  truths:
    - "Admin can list, create, edit, and delete Telegram groups with all fields (name, chat_id, sheet_id, shift hours, timezone)"
    - "Admin can list, create, edit, and delete employees with all fields (name, telegram_user_id, employee_code)"
    - "Admin can assign/unassign employees to groups and set sheet_row via GroupEmployee bindings"
    - "Admin can list, create, edit, and delete caption rules (group, pattern, employee)"
    - "Shift time windows (shift_start_hour, shift_end_hour) are editable on the group form"
  artifacts:
    - path: "shifttracker/admin/routers/groups.py"
      provides: "TelegramGroup CRUD endpoints"
      exports: ["router"]
    - path: "shifttracker/admin/routers/employees.py"
      provides: "Employee CRUD + GroupEmployee binding endpoints"
      exports: ["router"]
    - path: "shifttracker/admin/routers/caption_rules.py"
      provides: "CaptionRule CRUD endpoints"
      exports: ["router"]
    - path: "tests/test_admin_crud.py"
      provides: "CRUD integration tests"
  key_links:
    - from: "shifttracker/admin/router.py"
      to: "shifttracker/admin/routers/groups.py"
      via: "admin_router.include_router(groups.router, prefix='/groups')"
      pattern: "include_router.*groups"
    - from: "shifttracker/admin/routers/groups.py"
      to: "shifttracker/db/models.py"
      via: "select(TelegramGroup)"
      pattern: "TelegramGroup"
    - from: "shifttracker/admin/routers/employees.py"
      to: "shifttracker/db/models.py"
      via: "select(Employee), select(GroupEmployee)"
      pattern: "GroupEmployee"
---

<objective>
Implement full CRUD for Groups, Employees, Caption Rules, and Group-Employee bindings with server-rendered templates.

Purpose: Administrators need to configure the system (groups, employees, identification rules, shift windows) before the bot can process messages correctly. This plan delivers all configuration management.
Output: Working CRUD pages for Groups, Employees, Caption Rules, and Group-Employee bindings with full test coverage.
</objective>

<execution_context>
@C:/Users/nekro/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nekro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-admin-interface/03-CONTEXT.md
@.planning/phases/03-admin-interface/03-RESEARCH.md
@.planning/phases/03-admin-interface/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — auth, deps, templates, router -->

From shifttracker/admin/deps.py:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_factory() as session:
        yield session
```

From shifttracker/admin/auth.py:
```python
def require_session(request: Request):
    # Checks request.session["user"], raises 303 redirect if absent
    ...
```

From shifttracker/admin/router.py:
```python
from fastapi.templating import Jinja2Templates
from pathlib import Path
TEMPLATES_DIR = Path(__file__).parent.parent / "templates"
templates = Jinja2Templates(directory=str(TEMPLATES_DIR))
admin_router = APIRouter()
```

From shifttracker/db/models.py (with relationships from Plan 01):
```python
class Employee(Base):
    id, name, telegram_user_id, employee_code, created_at
    group_bindings: Mapped[list["GroupEmployee"]] = relationship(back_populates="employee")

class TelegramGroup(Base):
    id, chat_id, name, shift_start_hour, shift_end_hour, timezone, is_active, sheet_id, sheet_name, created_at
    group_employees: Mapped[list["GroupEmployee"]] = relationship(back_populates="group")

class GroupEmployee(Base):
    id, group_id, employee_id, sheet_row
    group: Mapped["TelegramGroup"] = relationship(back_populates="group_employees")
    employee: Mapped["Employee"] = relationship(back_populates="group_bindings")

class CaptionRule(Base):
    id, group_id, pattern, employee_id
    group: Mapped["TelegramGroup"] = relationship(...)
    employee: Mapped["Employee"] = relationship(...)
```

From tests/conftest.py (from Plan 01):
```python
@pytest.fixture
def test_client():
    # Synchronous TestClient with in-memory SQLite and SessionMiddleware
    # Patches async_session_factory for test isolation
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Groups and Employees CRUD routers + templates</name>
  <files>
    shifttracker/admin/routers/groups.py,
    shifttracker/admin/routers/employees.py,
    shifttracker/admin/router.py,
    shifttracker/templates/admin/groups/list.html,
    shifttracker/templates/admin/groups/form.html,
    shifttracker/templates/admin/employees/list.html,
    shifttracker/templates/admin/employees/form.html,
    shifttracker/templates/admin/employees/_bindings.html,
    tests/test_admin_crud.py
  </files>
  <read_first>
    shifttracker/admin/router.py,
    shifttracker/admin/auth.py,
    shifttracker/admin/deps.py,
    shifttracker/db/models.py,
    tests/conftest.py,
    .planning/phases/03-admin-interface/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test: POST /admin/groups/add creates a TelegramGroup in DB and redirects 303 to /admin/groups/
    - Test: GET /admin/groups/ lists all groups with name and chat_id visible
    - Test: POST /admin/groups/{id}/edit updates group fields (including shift_start_hour, shift_end_hour)
    - Test: DELETE /admin/groups/{id} via htmx returns 200 and group is removed from DB
    - Test: POST /admin/employees/add creates an Employee and redirects
    - Test: GET /admin/employees/ lists all employees
    - Test: POST /admin/employees/{id}/edit updates employee fields
    - Test: DELETE /admin/employees/{id} returns 200
    - Test: POST /admin/employees/{emp_id}/bindings/add assigns employee to group with sheet_row
    - Test: DELETE /admin/employees/{emp_id}/bindings/{binding_id} removes binding
  </behavior>
  <action>
    1. Create shifttracker/admin/routers/groups.py:
       - `router = APIRouter(prefix="/groups", tags=["groups"])`
       - GET "/" — list all TelegramGroup ordered by name. Render admin/groups/list.html with groups list.
       - GET "/add" — render admin/groups/form.html with empty form (group=None).
       - POST "/add" — accept Form fields: name (str), chat_id (int), sheet_id (str, optional), sheet_name (str, default "Sheet1"), shift_start_hour (int, default 6), shift_end_hour (int, default 22), timezone (str, default "Europe/Moscow"). Create TelegramGroup, commit, RedirectResponse("/admin/groups/", 303).
       - GET "/{group_id}/edit" — load TelegramGroup by id (404 if not found), render form.html with group object.
       - POST "/{group_id}/edit" — update all fields, commit, redirect to list.
       - DELETE "/{group_id}" — delete group, commit, return Response(status_code=200) for htmx outerHTML swap.

    2. Create shifttracker/admin/routers/employees.py:
       - `router = APIRouter(prefix="/employees", tags=["employees"])`
       - GET "/" — list all Employee ordered by name. Render admin/employees/list.html.
       - GET "/add" — render admin/employees/form.html with employee=None.
       - POST "/add" — accept Form: name (str), telegram_user_id (int, optional), employee_code (str, optional). Create Employee, commit, redirect.
       - GET "/{employee_id}/edit" — load Employee + their GroupEmployee bindings (eager load via selectinload or explicit query). Also load all TelegramGroup for the binding dropdown. Render form.html.
       - POST "/{employee_id}/edit" — update fields, commit, redirect.
       - DELETE "/{employee_id}" — delete employee, commit, return 200.
       - POST "/{employee_id}/bindings/add" — accept Form: group_id (uuid), sheet_row (int, optional). Create GroupEmployee. Return partial _bindings.html for htmx swap or redirect.
       - DELETE "/{employee_id}/bindings/{binding_id}" — delete GroupEmployee, return 200.

    3. Update shifttracker/admin/router.py:
       - Import groups and employees routers.
       - `admin_router.include_router(groups_router.router, dependencies=[Depends(require_session)])`
       - `admin_router.include_router(employees_router.router, dependencies=[Depends(require_session)])`

    4. Create templates (all extend base.html):
       - admin/groups/list.html: table with columns (Name, Chat ID, Sheet ID, Shift Window, Timezone, Actions). Each row has Edit link + Delete button (hx-delete, hx-target="closest tr", hx-swap="outerHTML", hx-confirm). "Add Group" button links to /admin/groups/add.
       - admin/groups/form.html: form with all fields. POST to /admin/groups/add or /admin/groups/{id}/edit based on whether group is set. shift_start_hour and shift_end_hour as number inputs (0-23).
       - admin/employees/list.html: table (Name, Telegram User ID, Employee Code, Actions). Add Employee button.
       - admin/employees/form.html: form with fields. Below the form, show "Group Bindings" section if editing (not adding). This section shows existing bindings and an "Add Binding" form.
       - admin/employees/_bindings.html: partial template for the bindings table body — used by htmx swap after adding/removing a binding.

    5. Create tests/test_admin_crud.py:
       - All tests use `test_client` fixture.
       - Helper: `login(client)` — POSTs to /admin/login with default creds.
       - test_group_crud: create group -> list (assert name in response) -> edit (change name) -> list (assert new name) -> delete (assert 200).
       - test_group_shift_window: create group with shift_start_hour=20, shift_end_hour=8 -> edit page shows those values.
       - test_employee_crud: create employee -> list -> edit -> delete.
       - test_group_employee_binding: create group, create employee, add binding with sheet_row=5 -> verify binding exists -> delete binding.
  </action>
  <verify>
    <automated>pytest tests/test_admin_crud.py -x --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TelegramGroup" shifttracker/admin/routers/groups.py
    - grep -q "shift_start_hour" shifttracker/admin/routers/groups.py
    - grep -q "Employee" shifttracker/admin/routers/employees.py
    - grep -q "GroupEmployee" shifttracker/admin/routers/employees.py
    - grep -q "include_router" shifttracker/admin/router.py
    - grep -q "test_group_crud" tests/test_admin_crud.py
    - grep -q "test_employee_crud" tests/test_admin_crud.py
    - grep -q "test_group_employee_binding" tests/test_admin_crud.py
  </acceptance_criteria>
  <done>
    Groups CRUD works (list/add/edit/delete) with shift window fields. Employees CRUD works with group binding management. All CRUD tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Caption Rules CRUD router + templates + tests</name>
  <files>
    shifttracker/admin/routers/caption_rules.py,
    shifttracker/admin/router.py,
    shifttracker/templates/admin/caption_rules/list.html,
    shifttracker/templates/admin/caption_rules/form.html,
    tests/test_admin_crud.py
  </files>
  <read_first>
    shifttracker/admin/routers/groups.py,
    shifttracker/admin/routers/employees.py,
    shifttracker/admin/router.py,
    shifttracker/db/models.py
  </read_first>
  <behavior>
    - Test: POST /admin/caption-rules/add creates a CaptionRule with correct group_id, employee_id, pattern
    - Test: GET /admin/caption-rules/ lists rules with group name, employee name, pattern
    - Test: POST /admin/caption-rules/{id}/edit updates pattern
    - Test: DELETE /admin/caption-rules/{id} removes rule
  </behavior>
  <action>
    1. Create shifttracker/admin/routers/caption_rules.py:
       - `router = APIRouter(prefix="/caption-rules", tags=["caption-rules"])`
       - GET "/" — list CaptionRule with joined group.name and employee.name (use selectinload or explicit join). Render admin/caption_rules/list.html.
       - GET "/add" — render form with group and employee dropdowns (load all TelegramGroup and Employee for selects). Render admin/caption_rules/form.html.
       - POST "/add" — accept Form: group_id (uuid), employee_id (uuid), pattern (str). Create CaptionRule, commit, redirect 303.
       - GET "/{rule_id}/edit" — load CaptionRule, load groups + employees for dropdowns. Render form.html.
       - POST "/{rule_id}/edit" — update fields, commit, redirect.
       - DELETE "/{rule_id}" — delete, commit, return 200 for htmx.

    2. Update shifttracker/admin/router.py — include caption_rules router with require_session dependency.

    3. Create templates:
       - admin/caption_rules/list.html: table (Group, Employee, Pattern, Actions). Add Rule button.
       - admin/caption_rules/form.html: form with group dropdown, employee dropdown, pattern text input.

    4. Add to tests/test_admin_crud.py:
       - test_caption_rule_crud: create group + employee first, then create rule -> list (assert pattern in text) -> edit -> delete.
  </action>
  <verify>
    <automated>pytest tests/test_admin_crud.py -x --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CaptionRule" shifttracker/admin/routers/caption_rules.py
    - grep -q "caption-rules" shifttracker/admin/router.py
    - grep -q "test_caption_rule_crud" tests/test_admin_crud.py
  </acceptance_criteria>
  <done>
    Caption rules CRUD works with group and employee selection. All CRUD tests pass including the new caption rule test.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_admin_crud.py -x --timeout=10` — all CRUD tests pass
- `pytest tests/ -x --timeout=10` — full suite still green
- Groups: list/add/edit/delete with shift window fields
- Employees: list/add/edit/delete with group binding management
- Caption Rules: list/add/edit/delete with group+employee selection
</verification>

<success_criteria>
All five admin CRUD requirements covered: ADMIN-01 (groups), ADMIN-02 (employees), ADMIN-03 (group-employee bindings), ADMIN-04 (shift time windows on group form), ADMIN-05 (caption rules + telegram account on employee). All CRUD operations persist to database and are verified by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-interface/03-02-SUMMARY.md`
</output>
